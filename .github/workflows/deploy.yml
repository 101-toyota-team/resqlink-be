name: Deploy Backend

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: DEVELOPMENT
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        working-directory: ./backend
        run: npm install

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Verify Secrets
        run: |
          if [ -z "${{ secrets.SUPABASE_PROJECT_ID }}" ]; then echo "❌ SUPABASE_PROJECT_ID is missing"; exit 1; fi
          if [ -z "${{ secrets.SUPABASE_ACCESS_TOKEN }}" ]; then echo "❌ SUPABASE_ACCESS_TOKEN is missing"; exit 1; fi
          if [ -z "${{ secrets.CLOUDFLARE_API_TOKEN }}" ]; then echo "❌ CLOUDFLARE_API_TOKEN is missing"; exit 1; fi
          if [ -z "${{ secrets.UPSTASH_REDIS_REST_URL }}" ]; then echo "❌ UPSTASH_REDIS_REST_URL is missing"; exit 1; fi
          if [ -z "${{ secrets.UPSTASH_REDIS_REST_TOKEN }}" ]; then echo "❌ UPSTASH_REDIS_REST_TOKEN is missing"; exit 1; fi
          if [ -z "${{ secrets.SUPABASE_SECRET_KEY }}" ]; then echo "❌ SUPABASE_SECRET_KEY is missing"; exit 1; fi
          if [ -z "${{ secrets.GOOGLE_MAPS_API_KEY }}" ]; then echo "❌ GOOGLE_MAPS_API_KEY is missing"; exit 1; fi

      - name: Deploy Supabase Migrations
        run: |
          supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
          supabase db push --include-all
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}

      - name: Push Secrets to Cloudflare Worker
        working-directory: ./backend
        run: |
          npx wrangler secret put UPSTASH_REDIS_REST_URL <<< "${{ secrets.UPSTASH_REDIS_REST_URL }}"
          npx wrangler secret put UPSTASH_REDIS_REST_TOKEN <<< "${{ secrets.UPSTASH_REDIS_REST_TOKEN }}"
          npx wrangler secret put SUPABASE_SECRET_KEY <<< "${{ secrets.SUPABASE_SECRET_KEY }}"
          npx wrangler secret put GOOGLE_MAPS_API_KEY <<< "${{ secrets.GOOGLE_MAPS_API_KEY }}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy Cloudflare Worker
        working-directory: ./backend
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

# TODO: Add PRODUCTION environment deployment
# When ready to deploy main branch to production:
# 1. Create a "PRODUCTION" environment in GitHub settings
# 2. Configure it to deploy on 'main' branch
# 3. Add PRODUCTION secrets (same variables as DEVELOPMENT)
# 4. Add a second deploy job that targets the 'main' branch and PRODUCTION environment
